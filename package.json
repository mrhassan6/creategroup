{
  "name": "gc-agent-root",
  "version": "1.0.0",
  "description": "WhatsApp Group Agent with APK & Pairing Code Manager",
  "scripts": {
    "dev:server": "cd server && npm start",
    "dev:client": "cd client && npm run dev",
    "build:client": "cd client && npm run build",
    "build": "cd client && npm install && chmod +x ./node_modules/.bin/* && npm run build && cd ../server && npm install"
    "start": "cd server && npm start"
    
  }
 
}
